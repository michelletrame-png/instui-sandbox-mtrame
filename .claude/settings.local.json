{
  "permissions": {
    "allow": [
      "mcp__figma__get_figma_data",
      "WebFetch(domain:github.com)",
      "WebSearch",
      "Bash(gh api:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty'); if [ -n \"$FILE\" ]; then ESLINT_OUT=$(cd /Users/tom.murphy/VibeCoding/instui-sandbox && npx eslint \"$FILE\" --max-warnings 0 2>&1); STATUS=$?; if [ $STATUS -ne 0 ]; then echo \"$ESLINT_OUT\" >&2; exit 2; fi; fi"
          }
        ]
      }
    ]
  }
}
